# Contributing

Before opening a pull request:

```bash
npm run lint
npm run typecheck
npm run test
npm run build
```

Use conventional commits and keep changes inside the relevant feature boundary. Add tests for business logic, route handlers, and user-facing behavior. For UI work, verify keyboard navigation, screen reader labels, focus states, reduced motion, and mobile layouts.
